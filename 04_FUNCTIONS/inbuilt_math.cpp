#include<bits/stdc++.h>
using namespace std;
 
int main(){
   int a ,b;
   cin >> a >> b;

   int minimum = min(a,b);
   cout << minimum << endl;

    int maximum = max(a,b);
   cout << maximum; 


    return 0;
}